<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Корни Подсолнуха</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="http://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/css/materialize.min.css">

    <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/js/materialize.min.js"></script>

</head>

<body>
    <div class="row">
        {{#each orders}}
        <div class="col s12 center">
            <div class="row">
                <div class="col">
                    <p>Имя:<span class="bold">{{ this.user.username }}</span></p>
                </div>
                <div class="col">
                    <p>Тел.:<span class="bold">{{ this.user.phone }}</span></p>
                </div>
                <div class="col">
                    <p>Тел2.:<span class="bold">{{ this.user.secondphone }}</span></p>
                </div>
                <div class="col">
                    <p>Адрес:<span class="bold">{{ this.user.adress }}</span></p>
                </div>
                <div class="col">
                    <p>Скидка:<span class="bold">{{ this.user.discount }}%</span></p>
                </div>
                <div class="col">
                    <p>Пол.:<span class="bold">{{empty this.user.note }}</span></p>
                </div>
                <div class="col">
                    <p>Адм.:<span class="bold">{{empty this.adminNote }}</span></p>
                </div>
            </div>
        </div>
        <table class="bordered striped centered">
            <thead>
                <tr>
                    <th class="center">№</th>
                    <th>Фото</th>
                    <th>Название</th>
                    <th>Цена</th>
                    <th>Количество</th>
                    <th>Сумма</th>
                </tr>
            </thead>
            <tbody>
                {{#each this.order}}
                <tr>
                    <td>{{incr @index}}</td>
                    <td>
                        <img class="img_list" src="/images/{{img this.image }}" alt="foto">
                    </td>
                    <td>
                        <p class="bold">{{this.name}}</p>
                    </td>
                    <td>
                        <p class="bold">{{this.price}} грн.</p>
                    </td>
                    <td>
                        <p class="bold">{{this.count}} шт.</p>
                    </td>
                    <td>
                        <p class="bold">{{multiply this.count this.price}} грн.</p>
                    </td>
                </tr>
                {{/each}}
                <tr>
                    <td colspan="4">
                        <p class="bold">Итого</p>
                    </td>
                    <td colspan="2" class="bold">{{result this.order}} грн.</td>
                </tr>
            </tbody>
        </table>
        <div class="margin divider black"></div>
        {{/each}}
    </div>
</body>

</html>

<style>
    .img_list {
        height: 50px;
        width: 50px;
    }

    td {
        padding: 0;
    }

    .margin {
        margin: 10px 0;
    }

    .bold {
        font-weight: bold;
    }
</style>

<script>
    window.print()

</script>